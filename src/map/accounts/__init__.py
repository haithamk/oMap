"""
Acount module is responsible for all acount related operations.
It relys on django.contrib.auth.models.User and an override of some of it's
behaviour.

The module currently supports creation of new user process, logging-in and out


"""